[project]
name = "mcp-email-server"
version = "0.0.1"
description = "Full-featured, multi-account MCP email server for Windows, macOS, and Linux—read, search, organize, and send email via IMAP and SMTP."
authors = [{ name = "wh1isper", email = "jizhongsheng957@gmail.com" }]
readme = "README.md"
keywords = ["MCP", "MCP server", "Model Context Protocol", "email", "email server", "IMAP", "SMTP", "multi-account"]
requires-python = ">=3.11,<4.0"
classifiers = [
    "Intended Audience :: Developers",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "aioimaplib>=2.0.1",
    "aiosmtplib>=4.0.0",
    "anyio>=4.5",
    "beautifulsoup4>=4.14.3",
    "click>=8.0.0",
    "filelock>=3.32.2",
    "jinja2>=3.1.5",
    "keyring>=25.0",
    "loguru>=0.7.3",
    "markdown>=3.5",
    "mcp[cli]>=1.23.0,<2",
    "pydantic>=2.11.0",
    "pydantic-settings[toml]>=2.11.0",
    "pywin32>=312 ; sys_platform == 'win32'",
    "starlette>=0.40",
    "tomli-w>=1.2.0",
    "typer>=0.15.1",
    "tzdata>=2025.2 ; sys_platform == 'win32'",
    "uvicorn>=0.30",
]

[project.scripts]
mcp-email-server = "mcp_email_server.cli:app"
mcp-email-server-plugin = "mcp_email_server.cli:plugin_stdio"

[project.urls]
Homepage = "https://mcp-email-server.wh1isper.top/"
Repository = "https://github.com/Wh1isper/mcp-email-server"
Documentation = "https://mcp-email-server.wh1isper.top/"

[dependency-groups]
dev = [
    "uv==0.11.29",
    "coverage[toml]>=7.10.0",
    "pytest>=7.2.0",
    "pytest-asyncio>=0.25.3",
    "pre-commit>=2.20.0",
    "deptry>=0.22.0",
    "pyright>=1.1.0",
    "pytest-cov>=7.0.0",
    "ruff>=0.9.2",
    "mkdocs>=1.4.2",
    "mkdocs-material>=8.5.10",
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
artifacts = ["mcp_email_server/web_ui/static/**"]
exclude = [
    "frontend/node_modules/**",
    "frontend/dist/**",
    "frontend/playwright-report/**",
    "frontend/test-results/**",
    "frontend/*.tsbuildinfo",
    "**/__pycache__/**",
]

[tool.hatch.build.targets.wheel]
packages = ["mcp_email_server"]

[tool.hatch.build.targets.sdist]
include = [
    "/mcp_email_server",
    "/frontend",
    "/plugins",
    "/.agents",
    "/.claude-plugin",
    "/.github",
    "/.pre-commit-config.yaml",
    "/codecov.yaml",
    "/dev",
    "/tests",
    "/docs",
    "/spec",
    "/README.md",
    "/CONTRIBUTING.md",
    "/LICENSE",
    "/Makefile",
    "/mkdocs.yml",
    "/pyproject.toml",
    "/pytest.ini",
    "/uv.lock",
]

[tool.pyright]
include = ["mcp_email_server"]
exclude = ["**/__pycache__", "**/.*", ".venv"]
pythonVersion = "3.13"
typeCheckingMode = "standard"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.deptry.package_module_name_map]
tzdata = ["zoneinfo"]
pywin32 = [
    "ntsecuritycon",
    "win32api",
    "win32con",
    "win32file",
    "win32security",
]

[tool.ruff]
target-version = "py311"
line-length = 120
fix = true

[tool.ruff.lint]
select = [
    # flake8-2020
    "YTT",
    # flake8-bandit
    "S",
    # flake8-bugbear
    "B",
    # flake8-builtins
    "A",
    # flake8-comprehensions
    "C4",
    # flake8-debugger
    "T10",
    # flake8-simplify
    "SIM",
    # isort
    "I",
    # mccabe
    "C90",
    # pycodestyle
    "E",
    "W",
    # pyflakes
    "F",
    # pygrep-hooks
    "PGH",
    # pyupgrade
    "UP",
    # ruff
    "RUF",
    # tryceratops
    "TRY",
]
ignore = [
    # LineTooLong
    "E501",
    # DoNotAssignLambda
    "E731",
    # raise-vanilla-args
    "TRY003",
    # try-consider-else
    "TRY300",
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "S106", "SIM117"]
"tests/conftest.py" = ["E402"]

[tool.ruff.format]
preview = true

[tool.coverage.report]
fail_under = 80
precision = 2
skip_empty = true

[tool.coverage.run]
branch = true
parallel = true
patch = ["subprocess"]
source = ["mcp_email_server"]
omit = ["*/mcp_email_server/ui.py", "*/mcp_email_server/windows_security.py"]
